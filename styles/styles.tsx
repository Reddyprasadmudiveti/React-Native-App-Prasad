import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    headingText:{
        flex:1,
        fontSize:20,
        fontWeight:"bold",
    },
    headingContainer:{
        flex:1,
        justifyContent:"center",
        alignItems:"center",
        marginTop:20
      },
})
